language: elixir
otp_release:
  - 18.0
  - 19.0
elixir:
  - 1.1.0
  - 1.2.0
  - 1.3.0
env:
  - MIX_ENV=test
script:
  - mix local.hex --force
  - mix deps.get
  - mix compile --warnings-as-errors
  - mix test
